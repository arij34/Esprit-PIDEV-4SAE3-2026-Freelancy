apiVersion: v1
kind: Service
metadata:
  name: matching-service
  namespace: devops
spec:
  selector:
    app: matching-module
  ports:
    - protocol: TCP
      port: 8087
      targetPort: 8087
      nodePort: 30087
  type: NodePort