apiVersion: v1
kind: Service
metadata:
  name: blog-analytics-service
  namespace: freelancy
  labels:
    app: blog-analytics-service
spec:
  type: NodePort
  selector:
    app: blog-analytics-service
  ports:
    - protocol: TCP
      port: 8053
      targetPort: 8053
