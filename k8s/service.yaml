apiVersion: v1
kind: Service
metadata:
  name: matching-service
  namespace: freelancy
  labels:
    app: matching-module
spec:
  selector:
    app: matching-module
  ports:
    - name: http
      protocol: TCP
      port: 8087
      targetPort: 8087
      nodePort: 30087
  type: NodePort