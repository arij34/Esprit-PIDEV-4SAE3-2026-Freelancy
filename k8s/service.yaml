apiVersion: v1
kind: Service
metadata:
  name: smart-analysis-service
  namespace: freelancy
spec:
  selector:
    app: smart-analysis
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
  type: NodePort
