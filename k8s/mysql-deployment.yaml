apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-projet-pvc
  namespace: freelancy
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-projet
  namespace: freelancy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-projet
  template:
    metadata:
      labels:
        app: mysql-projet
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "root"
            - name: MYSQL_DATABASE
              value: projet_Module
            - name: MYSQL_USER
              value: projet_user    # ✅ différent de root
            - name: MYSQL_PASSWORD
              value: "projet_pass"  # ✅ mot de passe non vide
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mysql-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-storage
          persistentVolumeClaim:
            claimName: mysql-projet-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-projet-service
  namespace: freelancy
spec:
  selector:
    app: mysql-projet
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP
