# CD flow:
# Docker Hub image -> Kubernetes pulls image -> deployment inside namespace freelancy
apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog-analytics-service-deployment
  namespace: freelancy
  labels:
    app: blog-analytics-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blog-analytics-service
  template:
    metadata:
      labels:
        app: blog-analytics-service
    spec:
      containers:
        - name: blog-analytics-service
          image: pidevfreelancy/blog-analytics-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8053
          env:
            - name: SERVER_PORT
              value: "8053"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://192.168.56.1:3306/microservices_db?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false"
            - name: SPRING_DATASOURCE_USERNAME
              value: "root"
            - name: SPRING_DATASOURCE_PASSWORD
              value: ""
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://192.168.56.10:8761/eureka/"
            - name: MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE
              value: "health,info,prometheus"
            - name: MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS
              value: "always"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8053
            initialDelaySeconds: 30
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8053
            initialDelaySeconds: 45
            periodSeconds: 20
