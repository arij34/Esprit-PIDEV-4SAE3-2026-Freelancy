pipeline {
    agent any
    tools {
        maven "Maven3"
    }

    environment {
        MS_NAME            = "backend"
        IMAGE_NAME         = "pidevfreelancy/examquiz-service"
        IMAGE_TAG          = "latest"
        SONAR_PROJECT_KEY  = "freelancy-examquiz-service"
        SONAR_PROJECT_NAME = "examquiz-service"
        DOCKER_CREDS       = credentials('docker-hub-credentials')
    }

    stages {
        stage('1 - Checkout') {
            steps {
                git branch: 'ExamQuiz-Service',
                    credentialsId: 'github-credentials',
                    url: 'https://github.com/arij34/Esprit-PIDEV-4SAE3-2026-Freelancy.git'
                echo "Code retrieved from GitHub - branch ExamQuiz-Service"
            }
        }

        stage('2 - Build Maven') {
            steps {
                dir("${MS_NAME}") {
                    sh 'mvn clean package -DskipTests'
                    echo "Maven build completed: ${MS_NAME}"
                }
            }
        }

        stage('3 - Tests + JaCoCo') {
            steps {
                dir("${MS_NAME}") {
                    sh 'mvn test jacoco:report'
                    sh 'test -f target/site/jacoco/jacoco.xml'
                }
            }
            post {
                always {
                    junit "backend/target/surefire-reports/*.xml"
                }
                failure {
                    error 'Tests failed - stopping pipeline'
                }
            }
        }

          

          stage('SONARQUBE ANALYSIS') {
            steps {
                withSonarQubeEnv('sonar') {
                    sh """
                        mvn sonar:sonar \
                        -Dsonar.projectKey=SkillManagement \
                        -Dsonar.projectName=SkillManagement \
                        -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml
                    """
                }
            }
        }

        stage('5 - Docker Build') {
            steps {
                dir("${MS_NAME}") {
                    sh "docker build -t ${IMAGE_NAME}:${IMAGE_TAG} ."
                    echo "Docker image built: ${IMAGE_NAME}:${IMAGE_TAG}"
                }
            }
        }

        stage('6 - Docker Push') {
            steps {
                sh "echo ${DOCKER_CREDS_PSW} | docker login -u ${DOCKER_CREDS_USR} --password-stdin"
                sh "docker push ${IMAGE_NAME}:${IMAGE_TAG}"
                echo "Image published on Docker Hub: ${IMAGE_NAME}:${IMAGE_TAG}"
            }
        }
    }

    post {
        success {
            echo "CI Pipeline completed - image ${IMAGE_NAME}:${IMAGE_TAG} available on Docker Hub"
            build job: 'ExamQuizPipeline-CD', wait: false
        }
        failure {
            echo "CI Pipeline failed - check logs above"
        }
        always {
            sh "docker rmi ${IMAGE_NAME}:${IMAGE_TAG} || true"
            echo "CI Pipeline finished."
        }
    }
}
